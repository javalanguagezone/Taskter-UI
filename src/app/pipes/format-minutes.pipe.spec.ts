import { FormatMinutesPipe } from './format-minutes.pipe';

describe('FormatMinutesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMinutesPipe();
    expect(pipe).toBeTruthy();
  });
});
